/// <reference types="react" />
import { LabeledButtonType } from "labeled-button";
/** container for any index instance that needs to be rendered in the explore page */
export declare const TitleContainer: ({ title, children, buttons, }: {
    title?: string | undefined;
    children: any;
    buttons: LabeledButtonType[];
}) => JSX.Element;
