import { TypescriptIndex } from "code-types";
/**
 * calculates a path of a specific index file (doesn't check if it exists)
 *
 * DEPRECATED: Not needed because db saves it in other way now
 */
export declare const getIndexFilePath: (srcFilePath: string, indexInstance: keyof TypescriptIndex) => string | undefined;
//# sourceMappingURL=getIndexFilePath.d.ts.map