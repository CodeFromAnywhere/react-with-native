/**
 * simple sync function to check if a folder is the root of a project (not operation but a workspace)
 */
export declare const isProjectRoot: (folderPath: string) => boolean;
//# sourceMappingURL=isProjectRoot.d.ts.map