/**
Remove the token (if present), then add it again
 */
export declare const ensureToken: (name: string, newToken?: string, attachTokenToFilename?: boolean) => string;
//# sourceMappingURL=ensureToken.d.ts.map