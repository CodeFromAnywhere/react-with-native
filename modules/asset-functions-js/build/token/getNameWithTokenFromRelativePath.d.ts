/**
 * first gets the name from the relativePath
 *
 * then ensures the token is attached (if not already there)
 */
export declare const getNameWithTokenFromRelativePath: (relativePath: string, attachTokenToFilename?: boolean, newToken?: string) => string;
//# sourceMappingURL=getNameWithTokenFromRelativePath.d.ts.map